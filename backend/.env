# MeetIQ Backend — Local Environment
# Copy this file to .env and fill in your values

# ── Google Gemini (required — free tier at https://aistudio.google.com/apikey) ─
GEMINI_API_KEY=AIzaSyD015N4MlMHq4uAj5OWouzR4ghZkXK7CPc
GEMINI_MODEL=gemini-2.0-flash

# ── Database ──────────────────────────────────────────────────────────────────
# SQLite (default, no setup needed for local dev):
DATABASE_URL=sqlite+aiosqlite:////data/meetiq.db

# PostgreSQL (for production / Railway / Render):
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/meetiq
